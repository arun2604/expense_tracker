/* eslint-disable prettier/prettier */
import { View, Text } from 'react-native';
import React from 'react';

const ExpenseStatus = () => {
    return (
        <View>
            <Text>ExpenseStatus</Text>
        </View>
    );
};

export default ExpenseStatus;
